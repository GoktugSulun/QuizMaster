import { styled } from "@mui/material";

export const FullSizeLoadingWrapper = styled('div')({
   width: '100vw',
   height: '100vh',
   overflow: 'hidden',
   display: 'flex',
   justifyContent: 'center',
   alignItems: 'center',
})